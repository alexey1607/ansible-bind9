---
# OS-specific variables for Debian/Ubuntu
bind_packages:
  - bind9
  - bind9-dnsutils
  - bind9-doc
  - bind9-host
  - bind9-utils
  - bind9utils

bind_service_name: bind9
bind_config_path: /etc/bind
bind_user: bind
bind_group: bind
